@model IEnumerable<Claim>

@{
    ViewData["Title"] = "Manager Dashboard";
}

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col">
            <h2>Manager Dashboard</h2>
            <p class="text-muted">Approve or reject reviewed claims</p>
        </div>
    </div>

    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle"></i> @TempData["Message"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-circle"></i> @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="card">
        <div class="card-header bg-info text-white">
            <h4 class="card-title mb-0">
                <i class="fas fa-tasks"></i> Reviewed Claims (@Model.Count())
            </h4>
        </div>
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead class="table-dark">
                            <tr>
                                <th>Lecturer</th>
                                <th>Month</th>
                                <th>Year</th>
                                <th>Hours</th>
                                <th>Total Amount</th>
                                <th>Coordinator Review</th>
                                <th>Status</th>
                                <th>Submission Date</th>
                                <th>Documents</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var claim in Model)
                            {
                                <tr>
                                    <td>@claim.Lecturer?.User?.FirstName @claim.Lecturer?.User?.LastName</td>
                                    <td>@claim.Month</td>
                                    <td>@claim.Year</td>
                                    <td>@claim.HoursWorked</td>
                                    <td>R @claim.TotalAmount.ToString("N2")</td>
                                    <td>
                                        @if (!string.IsNullOrEmpty(claim.CoordinatorReview))
                                        {
                                            <span class="badge bg-info">Reviewed</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-warning">Pending</span>
                                        }
                                    </td>
                                    <td>
                                        <span class="badge bg-info">@claim.Status</span>
                                    </td>
                                    <td>@claim.SubmissionDate.ToString("dd/MM/yyyy")</td>
                                    <td>
                                        @if (claim.Documents?.Any() == true)
                                        {
                                            <span class="badge bg-success">
                                                <i class="fas fa-file"></i> @claim.Documents.Count
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">None</span>
                                        }
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <button type="button" class="btn btn-sm btn-outline-primary"
                                                    data-bs-toggle="modal" data-bs-target="#approvalModal@(claim.ClaimID)">
                                                <i class="fas fa-gavel"></i> Decide
                                            </button>
                                            <a href="@Url.Action("Details", "Claim", new { id = claim.ClaimID })"
                                               class="btn btn-sm btn-outline-info">
                                                <i class="fas fa-eye"></i> View
                                            </a>
                                        </div>

                                        <!-- Approval Modal -->
                                        <div class="modal fade" id="approvalModal@(claim.ClaimID)" tabindex="-1">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h5 class="modal-title">Final Approval</h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                                    </div>
                                                    <form method="post" action="@Url.Action("ManagerApprove", "Approval")">
                                                        <div class="modal-body">
                                                            <input type="hidden" name="claimId" value="@claim.ClaimID" />
                                                            <div class="mb-3">
                                                                <label class="form-label">Approval Notes</label>
                                                                <textarea name="notes" class="form-control" rows="3"
                                                                  placeholder="Enter your approval comments..."></textarea>
                                                            </div>
                                                            @if (!string.IsNullOrEmpty(claim.CoordinatorReview))
                                                            {
                                                                <div class="alert alert-info">
                                                                    <strong>Coordinator Review:</strong><br>
                                                                    @claim.CoordinatorReview
                                                                </div>
                                                            }
                                                        </div>
                                                        <div class="modal-footer">
                                                            <button type="submit" name="action" value="reject"
                                                                    class="btn btn-danger">
                                                                <i class="fas fa-times"></i> Reject
                                                            </button>
                                                            <button type="submit" name="action" value="approve"
                                                                    class="btn btn-success">
                                                                <i class="fas fa-check"></i> Approve
                                                            </button>
                                                        </div>
                                                    </form>
                                                </div>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-4">
                    <i class="fas fa-check-circle fa-3x text-success mb-3"></i>
                    <h5 class="text-success">All caught up!</h5>
                    <p class="text-muted">No reviewed claims awaiting approval.</p>
                </div>
            }
        </div>
    </div>
</div>